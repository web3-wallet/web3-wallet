import path from 'path';

export const CWD = process.cwd();
export const PACKAGES_DIR = path.join(process.cwd(), '../packages');
export const WALLETS_DIR = path.join(process.cwd(), '../packages/wallets');
export const PLUGINS_DIR = path.join(process.cwd(), '../packages/plugins');
export const BLOG_DIR = path.join(process.cwd(), '../blog');
export const DOCS_DIR = path.join(process.cwd(), '../docs');
